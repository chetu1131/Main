
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <style>
   thead{
    background-color: #47d3b5;
   }
     th,td{
        padding: 5px;
    }
    table , th , td {
      text-align: center;
        border: 1px solid black;
        border-radius: 5px;
    }
    .section{
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 6px;
     
    }
    .section a,p{
      outline: none;
      text-decoration: none;
      padding: 12px;
      margin: 3px;
      border: 2px solid black;
      border-radius: 12px;
      cursor: pointer;
      background-color: #67bdb8;
    }
  h1{
    text-align: center;
  }
 button{
  margin: 5px;
  padding: 10px;
  background-color: rgb(40, 156, 160);
 }
 .disabled{
  pointer-events: none;
  opacity: 0.8;
  background-color: rgb(151, 171, 172);
 }
  </style>
  <body>
    <div class="section">

      <% if(currentPage > 0) { %>
        <a href="/allattendance?page=<%=0  %>" ><<</a>
      <a href="/allattendance?page=<%= currentPage - 1  %>" ><</a>
      <% } else{ %>
        <a href="/allattendance?page=<%=0  %>"class="disabled" ><<</a>
        <a href="/allattendance?page=<%= currentPage  %>" class="disabled" ><</a>
        <% } %> 
        <p><%= currentPage%></p>

        <% if(currentPage < 9) { %>
          <a href="/allattendance?page=<%= ++currentPage %>" >></a>
          <a  href="/allattendance?page=<%=9 %>" >>></a>
        
        <% } else{ %>
          <a href="/allattendance?page=<%= currentPage %>"class="disabled" >></a>
          <a  href="/allattendance?page=<%=9  %>" class="disabled">>></a>
          <% } %> 
     </div>
 
     <form action="/allattendance">
      <label for="month">Month</label>
      <select name="month" id="month">
        <option value="12" <% if(month==12) {%> selected <%} %>>December</option>
        <option value="1"  <% if(month==1) {%> selected <%} %>>January</option>
        <option value="2"  <% if(month==2) {%> selected <%}%> >February</option>
      </select>
      <label for="year">Year</label>
      <select name="year" id="year">
        <option value="2023"  <% if(year==2023){%>selected<% } %>> 2023 </option>
        <option value="2024"  <% if(year==2024){%>selected<% } %>> 2024 </option>
      </select>
      <input type="submit" value="Get">
    </form> 
   
    <main ><h1>Attendance Master </h1>
      <form name="attedanceDetails" method="post" action="/allattendance">
        <table  >
            <thead>
              <tr>
                  <td>Student ID</td>
                  <td>First Name</td>
                  <td>Month</td>
                  <td>Year</td>
                  <td>Total Present</td>
                  <td>Attendance Percentage</td>
              </tr>
            </thead>
            <tbody >
              <tr>
                <% attedance.forEach(function (student){ %>
                    <tr>
                      <td><%=student.StudentID %></td>
                      <td><%=student.FirstName%></td>
                      <td><%=student.month%></td>  
                      <td><%=student.year%></td>  
                      <td><%=student.TotalPresent%></td>  
                      <td><%=student.Percentage%>%</td>   
                      <!-- <td><a href="/totalattendace">View more</a></td> -->
                    </tr>
                  <% }); %>
            </tr>
          </tbody>
        </table>
      </form>
  </body>
  <script>
     
  </script>
  
</html>

